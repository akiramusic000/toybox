-- Called once when the object is created or its script is loaded.
function init(self)
end

-- Called every tick (30 times per second) while the game is running.
function tick(self)
end

-- Called when this object overlaps another object. Both objects receive the callback.
function collision(self, other)
end

-- Called when a press or touch starts on this object.
function onPressStart(self)
end

-- Called when a press or touch ends on this object.
function onPressEnd(self)
end